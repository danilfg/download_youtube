
# 🎥 YouTube Playlist Downloader

Скрипт для скачивания видео и плейлистов с YouTube с поддержкой cookies и расшифровкой `n-signature` через Node.js.  
Работает на Windows, Linux и macOS.

---

## 📦 Установка

### 1️⃣ Клонировать репозиторий
```bash
git clone https://github.com/<your-username>/download_youtube.git
cd download_youtube
```

### 2️⃣ Создать и активировать виртуальное окружение

**macOS / Linux**

```bash
python3 -m venv .venv
source .venv/bin/activate
```

**Windows**

```bash
python -m venv .venv
.venv\Scripts\activate
```

### 3️⃣ Установить зависимости

```bash
pip install -r requirements.txt
```

---

## ⚙️ Настройка окружения

### Установи Node.js (если не установлен)

* **macOS (через brew):**

  ```bash
  brew install node
  ```

* **Linux (Debian/Ubuntu):**

  ```bash
  sudo apt install nodejs npm
  ```

* **Windows:**
  [Скачать LTS-версию Node.js](https://nodejs.org/) и установить с параметром *“Add to PATH”*.

Проверь:

```bash
node -v
```

---

## ▶️ Запуск скрипта

```bash
python youtube_d.py
```

Затем вставь ссылку на **видео** или **плейлист**, например:

```
https://www.youtube.com/playlist?list=PL8VxSMvNXhva7R8YshmJmkpSpMx_CgB-C
```

Видео будут сохранены в папку:

```
1_raw_videos/<Название_плейлиста>/<Название_видео>.mp4
```

---

## 🍪 Авторизация (для приватных или ограниченных видео)

Скрипт автоматически использует cookies из твоего браузера (**Chrome**).
Если не работает — можешь экспортировать вручную:

1. Установи расширение [Get cookies.txt LOCALLY](https://chromewebstore.google.com/detail/get-cookiestxt-locally/cclelndahbckbenkjhflpdbgdldlbecc)
2. Перейди на YouTube и войди в свой аккаунт.
3. Сохрани cookies в файл `cookies.txt` рядом со скриптом.
4. Включи использование cookies.txt (раскомментируй соответствующие строки в коде).

---

## 🧠 Примечания

* Работает с последней версией `yt-dlp` (2025.11+).
* Использует Node.js для обхода `EJS n-challenge` и SABR streaming.
* При каждом запуске автоматически подтягивает cookies из Chrome, если они доступны.

---

## 📁 Структура проекта

```
📂 download_youtube/
 ┣ 📜 youtube_d.py
 ┣ 📜 requirements.txt
 ┣ 📜 .gitignore
 ┗ 📁 1_raw_videos/       # скачанные видео
```

---

## 🧩 Совместимость

| ОС                           | Поддержка |
| ---------------------------- | --------- |
| macOS                        | ✅         |
| Linux (Debian, Ubuntu, Arch) | ✅         |
| Windows 10/11                | ✅         |

